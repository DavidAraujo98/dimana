# This test should fail

integer i = 0;
string u = "hello";

if(i == 0) then
    writeln "here";
fi;

# This should fail, because "1" isn't boolean
if (1) then
    writeln "here too";
elif if(u == "hello")  then
        writeln "there";
    fi;
fi;